[package]
name = "tui-line-editor"
version = "0.1.0"
edition = "2024"
description = "A small single- or multi-line text editor primitive (with selection and masking) for ratatui apps"
license = "MIT"
repository = "https://github.com/jhobern/paperboy"
keywords = ["ratatui", "tui", "editor", "input", "textarea"]
categories = ["command-line-interface"]

[dependencies]
ratatui = "0.30.2"
